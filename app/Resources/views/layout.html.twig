<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8" />
		<!-- création du bloc "title" avec "TCH : Mon site" comme contenu par défaut -->
        <title>{% block title %}TCH : Mon site{% endblock %}</title>
        {% block stylesheets %}
		<!-- appel du CSS situé dans web/css/bootstrap.css -->
		<link rel="stylesheet" href="{{asset('css/bootstrap.css')}}" type="text/css" />
		{% endblock %}
    </head>
    <body>
        <div class="container">
			<div id="header" class="hero-unit">
				<h1>
					<center>
						BLOG
					</center>
				</h1>
			</div>
			<div class="row">
				<div id="menu" class="span3">
					<h3>
						Menu
					</h3>
					<ul class="nav nav-pills nav-stacked">
					{# Utilisation de la fonction {{path}} pour faire des liens vers d'autres routes #}
						<li><a href="{{path('SiteTCHblog_accueil')}}">Accueil du blog</a></li>
						<li><a href="{{path('SiteTCHblogAbonne_abonne')}}">Abonnés</a></li>
						<li><a href="{{path('SiteTCHblog_ajouter')}}">ajouter un article</a></li>
					</ul>
					<!-- Inclusion de la méthode menu du contrôleur Blog du bundle TCHSiteBlogBundle avec l'argument nombre défini à 3 -->
					{{render(controller("SiteTCHBlogBundle:Blog:menu",{'nombre':3}))}}
				</div>
				<div id="content" class="span9">
					<!-- Création du bloc "body" sans contenu par défaut -->
					{% block body %}
					{% endblock %}
				</div>
			</div>
			<hr>
			<footer>
				<p>BTS SIO - Option SLAM</p>
			</footer>
		</div>
    </body>
</html>
